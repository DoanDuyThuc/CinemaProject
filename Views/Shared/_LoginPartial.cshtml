@using Microsoft.AspNetCore.Identity
@using App.Models

@inject SignInManager<AppUser> SignInManager
@inject UserManager<AppUser> UserManager

@if (SignInManager.IsSignedIn(User))
{
    @* <div class="btn-group">
        <button type="button" class="btn btn-primary">@UserManager.GetUserName(User)</button>
        <button type="button" class="btn btn-primary dropdown-toggle dropdown-toggle-split" data-bs-toggle="dropdown" aria-expanded="false">
            <span class="visually-hidden">Toggle Dropdown</span>
        </button>
        <ul class="dropdown-menu">
            <li class="Login_Menu-item">
                <i class="fa-regular fa-user"></i>
                <a class="dropdown-item" asp-area="Identity"  asp-controller="Manage" asp-action="Index" title="Manage">Tài khoản</a>
            </li>
            <li class="Login_Menu-item">
                <i class="fa-solid fa-arrow-right-from-bracket"></i>
                <form asp-area="Identity" asp-controller="Account" asp-action="LogOff" method="post" id="logoutForm" class="navbar-right">
                    <button type="submit" class="dropdown-item">Đăng xuất</button>
                </form>
            </li>
        </ul>
    </div> *@

    <div class="dropdown">
        <!-- Nút kích hoạt dropdown -->
        <button class="dropdown-btn">
            <i class="fa-solid fa-angle-down"></i>
        </button>

        <!-- Nội dung dropdown -->
        <ul class="dropdown-content">
            <!-- Các mục trong dropdown -->
            <li class="Login_Menu-item">
                <i class="fa-regular fa-user"></i>
                <a class="dropdown-item" asp-area="Identity"  asp-controller="Manage" asp-action="Index" title="Manage">Tài khoản</a>
            </li>
            <li class="Login_Menu-item">
                <i class="fa-solid fa-arrow-right-from-bracket"></i>
                <form asp-area="Identity" asp-controller="Account" asp-action="LogOff" method="post" id="logoutForm" class="navbar-right">
                    <button type="submit" class="dropdown-item">Đăng xuất</button>
                </form>
            </li>
        </ul>
    </div>
    
}
else
{
    <ul class="nav navbar-nav navbar-right">
        <li class="nav-item">
            <a class="nav-link" asp-area="Identity" asp-controller="Account" asp-action="Login">Đăng nhập</a>
        </li>
    </ul>
}